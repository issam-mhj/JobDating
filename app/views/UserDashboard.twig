<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YouCode Job Dating</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-gray-50 to-blue-50 min-h-screen">
    <!-- Header Section -->
    <header class="bg-white shadow-sm">
        <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
            <div class="flex justify-between items-center">
                <div class="flex items-center">
                    <img src="https://www.youcode.ma/images/logos/youcode.png" 
                        alt="YouCode Logo" 
                        class="h-10 w-auto filter invert"/>
                    <span class="ml-3 text-xl font-semibold text-gray-800">YouCode Job Dating</span>
                </div>
                <div class="flex space-x-4">
                    <a href="/logout" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
                        Log out
                    </a>
                </div>
            </div>
        </nav>
    </header>

    <!-- Hero Section -->
    <div class="bg-gradient-to-r from-blue-600 to-blue-800 text-white py-16">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center">
                <h1 class="text-4xl font-bold mb-4">Trouvez votre opportunité de carrière</h1>
                <p class="text-xl text-blue-100 mb-8">Participez aux sessions de Job Dating et rencontrez les meilleures entreprises</p>
                <!-- Search Bar -->
                <div class="max-w-2xl mx-auto">
                    <div class="relative rounded-lg shadow-lg">
                        <input type="text" placeholder="Rechercher une annonce..." 
                               class="w-full px-6 py-4 rounded-lg text-gray-900 focus:outline-none"/>
                        <button class="absolute right-3 top-3 bg-blue-600 text-white p-2 rounded-lg hover:bg-blue-700">
                            <i class="fas fa-search"></i>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {% for company in companies %}
    <p>{{ company.company_name }}</p>
    {% endfor %}

    <!-- Main Content -->
    <main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <!-- Stats Section -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
            <div class="bg-white rounded-xl shadow-sm p-6 text-center">
                <div class="text-blue-600 text-3xl mb-2">
                    <i class="fas fa-building"></i>
                </div>
                <div class="text-2xl font-bold text-gray-800">{{ totalCompanies }} +</div>
                <div class="text-gray-600">Entreprises Partenaires</div>
            </div>
            <div class="bg-white rounded-xl shadow-sm p-6 text-center">
                <div class="text-blue-600 text-3xl mb-2">
                    <i class="fas fa-briefcase"></i>
                </div>
                <div class="text-2xl font-bold text-gray-800">{{ totalAnnounces }}+</div>
                <div class="text-gray-600">Offres Disponibles</div>
            </div>
            <div class="bg-white rounded-xl shadow-sm p-6 text-center">
                <div class="text-blue-600 text-3xl mb-2">
                    <i class="fas fa-users"></i>
                </div>
                <div class="text-2xl font-bold text-gray-800">{{ usersNumber }}+</div>
                <div class="text-gray-600">Apprenants Placés</div>
            </div>
        </div>

        <!-- Announcements Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <!-- Announcement Card 1 -->
            {% for announce in announces %}
            <div class="bg-white rounded-xl shadow-md hover:shadow-lg transition-shadow duration-300">
                <div class="p-6">
                    <div class="flex items-center justify-between mb-4">
                        <div class="flex items-center">
                            <div class="bg-blue-600 h-12 w-12 rounded-lg flex items-center justify-center text-white text-xl">
                                {{ companyShortName }}
                            </div>
                            <div class="ml-3">
                                <h3 class="font-semibold text-gray-800">{{ companyName }}</h3>
                                <p class="text-sm text-gray-500">{{ JobNeeds }}</p>
                            </div>
                        </div>
                        <span class="bg-green-100 text-green-800 text-xs px-3 py-1 rounded-full">Nouveau</span>
                    </div>
                    <div class="space-y-3 mb-4">
                        <div class="flex items-center text-gray-600">
                            <i class="fas fa-map-marker-alt w-5"></i>
                            <span class="ml-2 text-sm">{{ location }}</span>
                        </div>
                        <div class="flex items-center text-gray-600">
                            <i class="fas fa-calendar w-5"></i>
                            <span class="ml-2 text-sm">Job Dating: {{ Date }} </span>
                        </div>
                    </div>
                    <p class="text-gray-600 text-sm mb-4">
{{ description }}
</p>
                    
                </div>
            </div>
            {% endfor %}
</div>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white mt-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div>
                    <h3 class="text-lg font-semibold mb-4">À propos</h3>
                    <p class="text-gray-400 text-sm">
                        YouCode organise des événements de Job Dating pour favoriser la rencontre entre les apprenants et les employeurs potentiels.
                    </p>
                </div>
                <div>
                    <h3 class="text-lg font-semibold mb-4">Contact</h3>
                    <div class="space-y-2 text-gray-400 text-sm">
                        <p><i class="fas fa-envelope mr-2"></i> contact@youcode.ma</p>
                        <p><i class="fas fa-phone mr-2"></i> +212 5XX-XXXXXX</p>
                    </div>
                </div>
                <div>
                    <h3 class="text-lg font-semibold mb-4">Suivez-nous</h3>
                    <div class="flex space-x-4">
                        <a href="#" class="text-gray-400 hover:text-white">
                            <i class="fab fa-linkedin text-2xl"></i>
                        </a>
                        <a href="#" class="text-gray-400 hover:text-white">
                            <i class="fab fa-twitter text-2xl"></i>
                        </a>
                        <a href="#" class="text-gray-400 hover:text-white">
                            <i class="fab fa-facebook text-2xl"></i>
                        </a>
                    </div>
                </div>
            </div>
            <div class="border-t border-gray-700 mt-8 pt-8 text-center text-gray-400 text-sm">
                © 2024 YouCode. Tous droits réservés.
            </div>
        </div>
    </footer>
</body>
</html>